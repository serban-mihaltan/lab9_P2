create table if not exists masini (numar_inmatriculare VARCHAR(20) PRIMARY KEY,
                                   marca VARCHAR(50),
                                   anul_fabricatiei INT,
                                   culoarea VARCHAR(30),
                                   nr_km INT);